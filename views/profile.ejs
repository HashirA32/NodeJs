<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Making a server</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        font-family: Arial, Helvetica, sans-serif;
      }

      body {
        background-color: rgb(0, 0, 10);
        color: #ffd700;
        padding: 80px;
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      h1 {
        font-size: 48px;
        text-transform: uppercase;
        padding: 80px;
        padding-bottom: 40px;
      }

      pre,
      li {
        font-size: 25px;
      }

      p {
        font-size: 38px;
        text-transform: uppercase;
        padding: 20px;
      }

      ol {
        width: fit-content;
      }
    </style>
  </head>
  <body>
    <% include partials/nav.ejs %>

    <h1>
      This is profile of<br />
      <%= person %>
    </h1>
    <pre>Age : <%= data.age %>          Contact : <%= data.contact %></pre>
    <p><strong> Hobbies </strong></p>
    <ol>
      <% data.hobby.forEach((item)=>{ %>
      <li><%= item %></li>
      <% }); %>
    </ol>
  </body>
</html>
